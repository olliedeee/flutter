<!-- we will render separate nav bars depending on whether or not the user is signed in. -->

<% if !user_signed_in? %>
    <%= render 'layouts/nav_guest' %>
<% else %>

    <%= render 'layouts/nav_user' %>
<% end %>
